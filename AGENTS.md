# AGENTS.md

## 项目定位

- 当前唯一主线：A 股低频多因子选股研究与回测。
- 第一版口径：月频决策和调仓，新开仓目标观察期约一个交易月；月末最后一个交易日收盘后生成信号，下一交易日执行，重新入选的股票可以继续持有。
- 第一版只做研究、统计和模拟回测，不接实盘、不执行真实下单或撤单。
- 当前唯一主实现文档是《A股多因子选股算法实现文档.md》，已合并原多因子方案与 A 股撮合、账户约束。
- 月频决策与月内每日账户处理分离：月末生成目标组合，月内每个交易日处理成交、公司行动、现金、持仓估值和风险暴露。
- 第一版按“三个因子、类别等权、无缓冲调仓、真实账户记账、成本后回测”的最小闭环推进，再扩展因子、缓冲、风险规则和动态权重。

## 目录边界

- 项目内容只能放在当前仓库根目录及其子目录中。
- 禁止把项目内容写到上级目录、`E:\` 根目录、`C:\` 或其他用户目录。
- 原始数据、标准化数据、派生数据、缓存、报告和临时导出物不得提交到 Git。
- 不提交账号、密码、Token、Cookie、快期账户信息或其他凭据。
- 不覆盖或回退与当前任务无关的用户改动。

## 默认工作流

1. 明确目标与验收边界。
2. 实现最短可运行纵向切片。
3. 只执行一次与改动风险匹配的定向验证。
4. 记录实际验证结果并提交。

未经明确要求，不采用 TDD、BDD、ATDD 或红绿重构，不先写测试再实现。BDD 只能用于简洁描述用户行为和验收示例，不能自动扩张成测试框架或测试矩阵。

测试不是交付目标。窄改动通常只运行一次最直接相关的测试、构建或 smoke；不得自动扩张为全量回归、浏览器多视口矩阵或无关扫描。

## 量化实现约束

- 所有特征、因子和标签必须按 `available_at` 对齐，禁止未来数据。
- 财务数据必须按公告时间或首次可见时间使用，不能按报告期回填。
- 因子计算、撮合和估值必须明确区分原始价格与复权价格。
- 历史涨跌停、费用、停牌、ST 和 IPO 规则必须按生效日期、市场板块和证券状态查询，不能使用单一当前规则覆盖全部历史区间。
- 回测必须包含 T+1、涨跌停、停牌、公司行动、费用、滑点和部分成交或未成交处理。
- 每次运行必须记录数据版本、因子版本、配置版本、`as_of` 时间和随机种子。
- 不在没有明确授权和仿真验证的情况下连接真实交易账户或发送订单。

## 代码与提交

- 优先沿用仓库现有结构、命名和依赖，不无理由引入新框架。
- 只改当前任务需要改的文件，避免无关重构和格式噪声。
- 提交前检查 `git diff`、实际运行结果和未跟踪文件。
- 提交信息应说明单一目的，不把无关修改混入同一个提交。
- 生成文件、数据文件、缓存和凭据不得进入提交。
