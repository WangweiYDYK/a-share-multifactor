# Todo

## 月频多因子回测：待补齐事项

`月末决策 -> 因子与预处理 -> 目标组合 -> 次日开盘撮合 -> 每日估值 -> 绩效与 IC`
已可离线跑通，以下为代码审查后确认的待补项。

### 回测口径

- [ ] 退市清算：持仓到退市日无法卖出，目前只记 `missing_quote` 拒单并按成本价挂账；需先确定清算价规则（最后收盘价、现金选择权或直接清零）再实现。
- [ ] 停牌期间估值：目前沿用最近收盘价，需确认长期停牌、退市整理期的估值口径。
- [ ] 公司行动：分红目前只增加现金，未对复权价格序列做再调整，需确认复权口径与账户账务的一致性。
- [ ] 涨跌停规则：比例目前是固定值，未按板块、ST 状态和生效日期查询；新股上市首日、科创板与创业板前 5 个交易日等特殊区间尚未覆盖。
- [ ] 费用与税制：佣金、印花税、过户费按固定值计算，需改为按生效日期查询（例如 2023-08 印花税下调）。
- [ ] 行业中性化方法：当前顺序是去极值 → 统一方向 → 标准化 → 行业去均值，与方法文档 §7.1 一致；文档 §7.4 提出的回归残差中性化（含市值与 beta）尚未实现，需先定口径再改，不要只调换顺序。
- [ ] 换手率均值把标签待定月份的 0 换手计入分母，需确认统计口径。

### 数据

- [ ] 补齐 `daily_basic`、`security_status`、`industry_membership` 三类历史数据；现有 BaoStock/Tushare demo 只能覆盖 `trade_calendar`、`security_master`、`daily_prices`。
- [ ] 接入复权因子与公司行动的权威来源。
- [ ] 财务数据按公告时间或首次可见时间接入，不能按报告期回填。
- [ ] 接入指数历史成分，用于基准与成分范围研究。
- [ ] 接入分析师预期数据（低优先级）。

### 研究扩展

- [ ] 因子扩充，并补充 IC 衰减、分位组合、因子相关性与冗余检验。
- [ ] 缓冲名单、个股与行业上限的参数敏感性，以及约束冲突时的取舍顺序。
- [ ] 基准与超额收益、Brinson 归因、成本敏感性和参数稳健性。
- [ ] 多轮回测结果汇总对比，形成跨 run 的指标表。
- [ ] 把月度评价做成可离线打开的 HTML 报告，对齐现有股票池报告的形式。

### 工程清理

- [ ] `data/storage.py` 存在未使用的 `Any`、`Mapping` 导入，清理前确认没有动态引用。

## 暂缓：QMT 数据源与执行接入

当前不接入 QMT，相关事项列为后续候选，不进入当前实现主线。

### 前置条件

- [ ] 确认本机 QMT 客户端、`xtquant` 或实际 SDK 的版本与接口能力。
- [ ] 确认可用的交易日历、日线、证券基础信息权限和数据字段口径。
- [ ] 确认 QMT 与 BaoStock、Tushare 在股票代码、成交量单位、成交额单位和复权口径上的差异。
- [ ] 将 `normalize.py` 中按 `raw.source` 分支的字段映射拆为按 provider 注册的映射器，再接入 QMT。

### 第一阶段：只做数据源

- [ ] 新增 `data/providers/qmt.py`。
- [ ] 只覆盖交易日历、日线和证券基础信息。
- [ ] 通过 `RawDataset -> CanonicalDataService -> Snapshot -> DataRepository` 主链路接入。
- [ ] 在快照 `manifest.json` 中记录 QMT 的实际来源、版本、时间范围和字段类型。
- [ ] 不对 QMT 暂不提供的财务、行业、退市、公司行动等数据做占位或推测。

### 后续阶段：模拟交易或实盘执行

- [ ] 与数据源适配器分开，单独建立 `execution/brokers/qmt.py` 或等价执行层模块。
- [ ] 先完成模拟交易和账务闭环验证，不以回测结果直接切换实盘。
- [ ] 在获得明确授权并通过仿真验证前，不连接真实交易账户，不发送真实订单。
