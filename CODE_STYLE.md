# A股多因子项目代码规范

## 1. 适用范围

本规范适用于数据采集、因子研究、组合构建、模拟撮合、账户记账和报告代码。项目只用于研究和模拟，不得在未单独授权的情况下增加实盘连接或真实下单逻辑。

## 2. 目录与职责

```text
src/ashare_multifactor/
  data/          数据源适配、字段标准化、时间对齐和质量检查
  factors/       因子定义、计算和横截面预处理
  portfolio/     目标组合和组合约束
  execution/     模拟账户、撮合、费用和历史交易规则
  reports/       研究结果和运行报告
scripts/         可直接运行的薄入口，不承载业务逻辑
tests/           与 src 目录对应的定向测试
config/          可提交的非敏感配置
data/            本地数据，禁止提交
reports/         本地报告，禁止提交
```

依赖方向必须保持为：数据层 -> 因子层 -> 组合层 -> 执行层/报告层。下层模块不得反向导入上层模块，策略代码不得绕过数据层直接调用供应商 SDK。

## 3. Python 约定

- 支持 Python 3.9 及以上版本，采用 4 空格缩进和 UTF-8 编码。
- 模块、函数和变量使用 `snake_case`，类使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`。
- 公共函数和数据模型必须有类型标注；跨层数据优先使用 `dataclass(frozen=True)`。
- 函数保持单一职责；出现多层分支或同时负责拉取、转换、保存时应拆分。
- 使用标准库 `logging`，库代码禁止使用 `print`；命令行入口可以输出简短结果。
- 捕获具体异常并补充上下文，禁止无记录地 `except Exception: pass`。
- 不使用可变对象作为默认参数，不依赖进程当前工作目录定位项目文件。

## 4. 数据与时间规范

每个可用于研究的数据集至少保留以下元数据：

```text
source          数据供应商
source_version  接口或提取器版本
retrieved_at    实际拉取时间，带时区
available_at    策略可使用时间，带时区
```

- 日期字段统一使用 `YYYY-MM-DD`，时间统一使用带时区的 ISO 8601 格式。
- A股业务时间使用 `Asia/Shanghai`，存储和比较时不得使用无时区时间。
- 财务数据的 `available_at` 必须来自公告时间或首次可见时间，禁止由报告期推导或回填。
- 原始价格、前复权价格、后复权价格必须使用不同字段或不同数据集，禁止覆盖原始价格。
- 数据源适配器返回项目标准字段；供应商原始字段可保留，但必须有清晰前缀或映射说明。
- 原始数据只追加或按明确分区替换。覆盖已有分区前必须能说明数据版本和影响范围。
- 缺失值不得静默填 0；异常、去重和剔除必须记录数量和原因。

## 5. 配置与凭据

- Token、密码和 Cookie 只通过环境变量或本地未提交文件传入。
- 可复现实验参数放入版本化配置，不能散落为脚本中的魔法数字。
- 每次研究运行记录数据版本、因子版本、配置版本、规则版本、`as_of` 和随机种子。

## 6. 模块接口

- 数据源客户端负责请求、限流错误转换和原始响应，不计算因子。
- 标准化函数必须是确定性的：相同输入与版本得到相同输出。
- 因子函数只能接收已经按 `available_at <= as_of` 截断的数据。
- 策略层只生成目标组合和理由，不直接修改账户。
- 撮合层显式返回成交、部分成交、拒绝和未成交原因，不假设订单必然成交。

## 7. 质量与验证

每个数据集至少检查主键唯一、必填字段、日期范围和记录数。新增业务逻辑时执行一次与风险匹配的定向验证：

- 数据适配：使用小日期范围做 smoke，并检查输出列和元数据。
- 因子：检查时间截断、方向、缺失率和一个手工可验证样本。
- 组合：检查权重、持仓数量和约束冲突。
- 撮合：检查 T+1、停牌、涨跌停、费用和未成交路径。

修复缺陷时应补充能复现该缺陷的测试。测试和示例不得访问真实交易账户。

## 8. 提交规范

- 一个提交只表达一个目的，提交前检查差异、运行结果和未跟踪文件。
- 推荐提交前缀：`data:`、`factor:`、`portfolio:`、`execution:`、`docs:`、`fix:`。
- 不提交数据、缓存、报告、凭据、虚拟环境或临时导出文件。

